coverage:
  status:
    project:
      default:
        target: 81%
        threshold: 1%
    patch:
      default:
        target: 90%
        threshold: 0%

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
