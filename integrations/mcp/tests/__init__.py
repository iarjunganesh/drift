"""Mocked-HTTP tests for the DRIFT MCP thin client.

These run separately from the backend coverage gate (see the Makefile
`test-integrations` target). No test performs a real network call or needs a
credential.
"""
